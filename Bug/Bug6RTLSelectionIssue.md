Priority: 4
Repro Steps:
    1. Click "Gateway Services" on the left to enter the service list page (/default/services).
    2. Click the "Create Service" button to enter the service creation page (/default/services/create).
    3. Fill in Arabic characters in the "Name" input box.
    4. Check the display effect in the service list or open delete popup.
    4. Try to select or highlight the name in the UI.
Expected:
    Selection and highlighting of Arabic characters should work normally.
Actual:
    Selection and highlighting of Arabic characters does not work properly.
Screenshot:
    ArabicSelection.png
Description:
    Arabic characters in Service names cause selection and highlighting issues in the UI.